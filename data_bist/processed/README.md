# Experimental Processed Folder

This folder is retained only for a possible future DataStore-based experiment. It would be regenerated by:

```powershell
py src\run_project_bist.py
```

The command requires `data_bist/raw/bist_monthly_ratios.csv`, which is not available for the current submission.
